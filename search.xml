<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>IO及reactor模型</title>
      <link href="/2024/03/18/IO%E5%8F%8Areactor%E6%A8%A1%E5%9E%8B/"/>
      <url>/2024/03/18/IO%E5%8F%8Areactor%E6%A8%A1%E5%9E%8B/</url>
      
        <content type="html"><![CDATA[<p>先介绍一下阻塞，非阻塞，同步，异步这四个概念。</p><h1 id="概念"><a href="#概念" class="headerlink" title="概念"></a>概念</h1><p>陈硕大神原话：<strong>在处理 IO 的时候，阻塞和非阻塞都是同步 IO。只有使用了特殊的 API 才是异步 IO。</strong></p><p>IO操作分为两个阶段：</p><ol><li>数据准备</li><li>数据读写<br>因为用户空间是不能直接操作硬件的，因此应用程序都要向内核空间读写数据。</li></ol><p><img src="https://pic3.zhimg.com/v2-89162cb4d5cc7fe4071ade36d3000d8a_r.jpg" alt="流程.jpg"></p><blockquote><p>一个典型的网络IO接口调用，分为两个阶段，分别是“数据就绪”和“数据读写”，数据就绪阶段分为阻塞和非阻塞，表现得结果就是，阻塞当前线程或是直接返回。 同步表示A向B请求调用一个网络IO接口时（或者调用某个业务逻辑API接口时），数据的读写都是由请求方A自己来完成的（不管是阻塞还是非阻塞）；异步表示A向B请求调用一个网络IO接口 时（或者调用某个业务逻辑API接口时），向B传入请求的事件以及事件发生时通知的方式，A就可以处理其它逻辑了，当B监听到事件处理完成后，会用事先约定好的通知方式，通知A处理结果。</p></blockquote><p><img src="https://s2.loli.net/2024/03/18/vSwsRidmb2qWJaN.png" alt="io.png"></p><h2 id="阻塞和非阻塞"><a href="#阻塞和非阻塞" class="headerlink" title="阻塞和非阻塞"></a>阻塞和非阻塞</h2><p>以一个recv函数读数据为例：<br>recv接收来自socket缓冲区的数据，当缓冲区没有数据可取时，recv会一直处于阻塞状态()，直到缓冲区至少又一个字节数据可读取，或者对端关闭，并读取所有数据后返回.</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">recv</span><span class="params">(<span class="type">int</span> sockfd, <span class="type">char</span> * buf, <span class="type">int</span> len, <span class="type">int</span> flags)</span></span>;</span><br><span class="line"></span><br><span class="line">sockfd：连接的fd</span><br><span class="line"></span><br><span class="line">buf：用于接收数据的缓冲区</span><br><span class="line"></span><br><span class="line">len：缓冲区长度</span><br><span class="line"></span><br><span class="line">flags：指定调用方式</span><br></pre></td></tr></table></figure><p>也就是说数据一直没准备好就会一直<strong>阻塞</strong>。<br>flags置为<code>MSG_DONTWAIT</code>则是<strong>非阻塞</strong>，不管有没有数据都会返回。非阻塞既然会立刻返回，那么一般用的时候都会加入循环，也就是隔一段时间去试试有没有接收到数据。</p><h2 id="同步和异步"><a href="#同步和异步" class="headerlink" title="同步和异步"></a>同步和异步</h2><p>阻塞和非阻塞其实都是发生在数据准备阶段，不管哪个方式，当我们读数据到自定义的buf中时，一定是我们自己在从内核空间往用户空间的buf中写数据。<br>而同步和异步的关键在于数据读写：异步接口会直接把buf给你写好返回给你（内核空间处理），关键在于你们直接要约定一个通知方式，当这个buf写好了要通知到你（信号，回调函数），因为你在执行这个接口后不会阻塞，只有当通知你是你再去处理。<br>在工作中也会讲同步异步不一定是IO操作但是思想是类似的，同步，就是调用某个东西是，调用方得等待这个调用返回结果才能继续往后执行。异步，和同步相反  调用方不会理解得到结果，而是在调用发出后调用者可用继续执行后续操作，被调用者通过状体来通知调用者，或者通过回掉函数来处理这个调用。</p><h1 id="Linux的五种IO模型"><a href="#Linux的五种IO模型" class="headerlink" title="Linux的五种IO模型"></a>Linux的五种IO模型</h1><h2 id="阻塞（blocking）"><a href="#阻塞（blocking）" class="headerlink" title="阻塞（blocking）"></a>阻塞（blocking）</h2><p><img src="https://s2.loli.net/2024/03/18/sgzvGSACIpnOYXa.png" alt="阻塞.png"></p><h2 id="非阻塞（non-blocking）"><a href="#非阻塞（non-blocking）" class="headerlink" title="非阻塞（non-blocking）"></a>非阻塞（non-blocking）</h2><p><strong>如果你连续做read操作而没有数据可读，此时程序不会阻塞起来等待数据准备就绪返回，read函数会返回一个错误EAGAIN</strong>，提示你的应用程序现在没有数据可读请稍后再试。<br><img src="https://s2.loli.net/2024/03/18/uycY4x6pJiLjUEA.png" alt="非阻塞.png"></p><h2 id="IO复用（IO-multiplexing）"><a href="#IO复用（IO-multiplexing）" class="headerlink" title="IO复用（IO multiplexing）"></a>IO复用（IO multiplexing）</h2><p><a href="./IO%E5%A4%9A%E8%B7%AF%E5%A4%8D%E7%94%A8.md">详细了解IO多路复用点此</a><br><img src="https://s2.loli.net/2024/03/18/BZoCxGr31OXu8HY.png" alt="io复用.png"></p><h2 id="信号驱动（signal-driven）"><a href="#信号驱动（signal-driven）" class="headerlink" title="信号驱动（signal-driven）"></a>信号驱动（signal-driven）</h2><p><img src="https://s2.loli.net/2024/03/18/MgTKXYrWklVPfFo.png" alt="信号驱动.png"></p><p>内核在第一个阶段是异步，在第二个阶段是同步；与非阻塞IO的区别在于它提供了消息通知机制，不需 要用户进程不断的轮询检查，减少了系统API的调用次数，提高了效率。<br>简单来说数据准备好了告诉你，读写还是要自己操作。</p><h2 id="异步（asynchronous）"><a href="#异步（asynchronous）" class="headerlink" title="异步（asynchronous）"></a>异步（asynchronous）</h2><p><img src="https://s2.loli.net/2024/03/18/U4ytm5TKpZ7uA6L.png" alt="异步.png"></p><h1 id="网络服务器设计理念"><a href="#网络服务器设计理念" class="headerlink" title="网络服务器设计理念"></a>网络服务器设计理念</h1><p>在这个多核时代，服务端网络编程如何选择线程模型呢？ 赞同libev作者的观点：one loop per thread is usually a good model，这样多线程服务端编程的问题就转换为如何设计一个高效且易于使用的event loop，然后每个线程run一个event loop就行了（当然线程间的同步、互斥少不了，还有其 它的耗时事件需要起另外的线程来做）。<br>event loop 是 non-blocking 网络编程的核心，在现实生活中，non-blocking 几乎总是和 IO-multiplexing 一起使用，原因有两点：  </p><ul><li>没有人真的会用轮询 (busy-pooling) 来检查某个 non-blocking IO 操作是否完成，这样太浪费 CPU资源了。</li><li>IO-multiplex 一般不能和 blocking IO 用在一起，因为 blocking IO 中 read()&#x2F;write()&#x2F;accept()&#x2F;connect() 都有可能阻塞当前线程，这样线程就没办法处理其他 socket 上的 IO 事件了。</li></ul><p>所以，当我们提到 non-blocking 的时候，实际上指的是 non-blocking + IO-multiplexing，单用其 中任何一个都没有办法很好的实现功能。</p><h1 id="Reactor模型"><a href="#Reactor模型" class="headerlink" title="Reactor模型"></a>Reactor模型</h1><blockquote><p>The reactor design pattern is an event handling pattern for handling service requests<br>delivered concurrently to a service handler by one or more inputs. The service handler<br>then demultiplexes the incoming requests and dispatches them synchronously to the<br>associated request handlers.</p></blockquote><p>重要组件：<strong>Event事件、Reactor反应堆、Demultiplex事件分发器、Evanthandler事件处理器</strong></p><p><img src="https://s2.loli.net/2024/03/18/5PvmXVR1FdQnqE2.png" alt="reactor.png"></p><ul><li>Event：事件的sockfd，事件类型（EPOLL_IN、EPOLL_OUT…）</li><li>Reactor：维护事件event以及事件处理EventHandler的集合</li><li>Demultiplex：多路复用分发器，监听sockfd上相应的事件，有事件发生的时候把事件列表返回给Reactor</li><li>EventHandler： 读取用户事件的请求read，反序列化，业务逻辑处理计算，对处理的结果序列化，send给用户返回一个响应</li></ul><p>在整个基于Reactor模型的网络服务交互阶段，首先我们把事件注册到反应堆上，也就是说，应用程序对这个事件感兴趣，请求反应堆帮忙来监听它所感兴趣的事件，并且在这个事件发生的时候调用相应预置的回调Handler</p><p>向反应堆注册事件，<strong>就是把这个事件event和对应的事件处理器EventHandler（一个event对应一个EventHandler）都给到reactor反应堆</strong>。反应堆存储着事件event以及事件处理的集合，可以添加更多选项，比如说事件添加的时间，事件响应的时间等等</p><p>reactor反应堆维护了事件event以及事件处理的集合，底层会通过epoll_ctl进行设置事件，调整事件，添加新的事件，修改已有事件，删除已有事件。然后启动反应堆，反应堆就会驱动事件分发器的启动，开启多路复用，实际上就是开启epoll_wait，然后服务器监听新用户的连接，或是已连接用户的读写事件</p><p>如果epoll_wait有监听到新的事件的产生，那么多路复用分发器Demultiplex就会把这个分发的事件给反应堆返回，因为事件发生了，reactor模型就要调用事件的处理器EventHandler，而这个EventHandler是注册在reactor反应堆的。反应堆收到多路复用分发器返回的事件后，reactor就会找到并调用event对应的EventHandler（一般用map表）处理事件。一般来说处理事件要么是建立连接，要么是读取数据处理并返回，需要进行数据的序列化和反序列化。</p><p><strong>muduo网络库使用的Multiple Reactors模型</strong><br><img src="https://img-blog.csdnimg.cn/933d7774592848c1bb629d6c96060de2.png?x-oss-process=image/watermark,type_d3F5LXplbmhlaQ,shadow_50,text_Q1NETiBAQnVnTWFrZXItc2hlbg==,size_20,color_FFFFFF,t_70,g_se,x_16" alt="reactor"></p><p>图中反应堆mainReactor和subReactor，实际上都是代表着我们上面说的Reactor和Demultiplex事件分发器，由他们监听具体事件的发生，维护事件event以及事件处理EventHandler的集合，调用相应的回调处理函数</p><p>多路事件分发器执行的就是epoll_wait，进行事件循环监听。对于经典的网络I&#x2F;O模型，一般使用多线程循环监听，比如我们使用一个线程专门循环监听新用户的连接，多个线程循环监听已连接用户的读写事件，如果再有其他耗时的操作，比如说传输文件，就再起一个线程去处理这些操作</p><p><img src="https://img-blog.csdnimg.cn/d979bc6a518d44deb7790f6da368fd2e.png?x-oss-process=image/watermark,type_d3F5LXplbmhlaQ,shadow_50,text_Q1NETiBAQnVnTWFrZXItc2hlbg==,size_20,color_FFFFFF,t_70,g_se,x_16" alt="reactorpoll"></p><p>muduo的特点是one loop per thread，有一个main reactor负载accept连接，然后把连接分发到某个sub reactor（采用<a href="https://blog.csdn.net/pzw_0612/article/details/47357221?spm=1001.2101.3001.6650.1&utm_medium=distribute.pc_relevant.none-task-blog-2~default~BlogCommendFromBaidu~Rate-1-47357221-blog-90265243.235%5Ev43%5Epc_blog_bottom_relevance_base8&depth_1-utm_source=distribute.pc_relevant.none-task-blog-2~default~BlogCommendFromBaidu~Rate-1-47357221-blog-90265243.235%5Ev43%5Epc_blog_bottom_relevance_base8&utm_relevant_index=2">round-robin</a>的方式来选择sub reactor），该连接的所有操作都在sub reactor所处的线程中完成。多个连接可能被分派到多个线程中，以充分列用CPU</p><p>Reactor Pool的线程数量是固定的，与CPU的核心数有关</p><p>如果有过多的耗时操作，可以提交到创建的ThreadPool中完成</p><p>转载：<br><a href="https://blog.csdn.net/qq_42500831/article/details/124118564">https://blog.csdn.net/qq_42500831/article/details/124118564</a></p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>IO多路复用</title>
      <link href="/2024/03/18/IO%E5%A4%9A%E8%B7%AF%E5%A4%8D%E7%94%A8/"/>
      <url>/2024/03/18/IO%E5%A4%9A%E8%B7%AF%E5%A4%8D%E7%94%A8/</url>
      
        <content type="html"><![CDATA[<p>目前支持I&#x2F;O多路复用的系统调用有<code>select，pselect，poll，epoll。</code>与多进程和多线程技术相比，<em>I&#x2F;O多路复用技术的最大优势是系统开销小，系统不必创建进程&#x2F;线程</em>，也不必维护这些进程&#x2F;线程，从而大大减小了系统的开销。</p><p>I&#x2F;O多路复用就是通过一种机制，<strong>一个进程可以监视多个描述符，一旦某个描述符就绪（一般是读就绪或者写就绪），能够通知程序进行相应的读写操作</strong>。但select，poll，epoll本质上都是同步I&#x2F;O，因为他们都需要在读写事件就绪后自己负责进行读写，也就是说这个读写过程是阻塞的，而异步I&#x2F;O则无需自己负责进行读写，异步I&#x2F;O的实现会负责把数据从内核拷贝到用户空间。</p><h1 id="select"><a href="#select" class="headerlink" title="select"></a>select</h1><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">select</span> <span class="params">(<span class="type">int</span> n, fd_set *readfds, fd_set *writefds, fd_set *exceptfds, <span class="keyword">struct</span> timeval *timeout)</span></span>;</span><br></pre></td></tr></table></figure><ul><li><p>readfds：内核检测该集合中的IO是否可读。如果想让内核帮忙检测某个IO是否可读，需要手动把文件描述符加入该集合。</p></li><li><p>writefds：内核检测该集合中的IO是否可写。同readfds，需要手动把文件描述符加入该集合。</p></li><li><p>exceptfds：内核检测该集合中的IO是否异常。同readfds，需要手动把文件描述符加入该集合。</p></li><li><p>nfds：以上三个集合中最大的文件描述符数值 + 1，例如集合是{0,1,5,10}，那么 maxfd 就是 11</p></li><li><p>timeout：用户线程调用select的超时时长。</p></li><li><p>设置成NULL，表示如果没有 I&#x2F;O 事件发生，则 select 一直等待下去。</p></li><li><p>设置为非0的值，这个表示等待固定的一段时间后从 select 阻塞调用中返回。</p></li><li><p>设置成 0，表示根本不等待，检测完毕立即返回。</p></li></ul><p>函数返回值：</p><ul><li>大于0：成功，返回集合中已就绪的IO总个数</li><li>等于-1：调用失败</li><li>等于0：没有就绪的IO</li></ul><p>从上述的select函数声明可以看出，fd_set本质是一个数组，为了方便我们操作该数组，操作系统提供了以下函数：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 将文件描述符fd从set集合中删除 </span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">FD_CLR</span><span class="params">(<span class="type">int</span> fd, fd_set *set)</span></span>; </span><br><span class="line"></span><br><span class="line"><span class="comment">// 判断文件描述符fd是否在set集合中 </span></span><br><span class="line"><span class="function"><span class="type">int</span>  <span class="title">FD_ISSET</span><span class="params">(<span class="type">int</span> fd, fd_set *set)</span></span>; </span><br><span class="line"></span><br><span class="line"><span class="comment">// 将文件描述符fd添加到set集合中 </span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">FD_SET</span><span class="params">(<span class="type">int</span> fd, fd_set *set)</span></span>; </span><br><span class="line"></span><br><span class="line"><span class="comment">// 将set集合中, 所有文件描述符对应的标志位设置为0</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">FD_ZERO</span><span class="params">(fd_set *set)</span></span>;</span><br></pre></td></tr></table></figure><p>select 函数监视的文件描述符分3类，分别是writefds、readfds、和exceptfds，<br>当用户process调用select的时候，select会将需要监控的readfds集合拷贝到内核空间（假设监控的仅仅是socket可读），然后遍历自己监控的skb(SocketBuffer)，挨个调用skb的poll逻辑以便检查该socket是否有可读事件，遍历完所有的skb后，如果没有任何一个socket可读，那么select会调用schedule_timeout进入schedule循环，使得process进入睡眠。如果在timeout时间内某个socket上有数据可读了，或者等待timeout了，则调用select的process会被唤醒，接下来select就是遍历监控的集合，挨个收集可读事件并返回给用户了，相应的伪码如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">select</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="type">int</span> nfds,</span></span></span><br><span class="line"><span class="params"><span class="function">    fd_set *readfds,</span></span></span><br><span class="line"><span class="params"><span class="function">    fd_set *writefds,</span></span></span><br><span class="line"><span class="params"><span class="function">    fd_set *exceptfds,</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="keyword">struct</span> timeval *timeout)</span></span>;</span><br><span class="line"><span class="comment">// nfds:监控的文件描述符集里最大文件描述符加1</span></span><br><span class="line"><span class="comment">// readfds：监控有读数据到达文件描述符集合，传入传出参数</span></span><br><span class="line"><span class="comment">// writefds：监控写数据到达文件描述符集合，传入传出参数</span></span><br><span class="line"><span class="comment">// exceptfds：监控异常发生达文件描述符集合, 传入传出参数</span></span><br><span class="line"><span class="comment">// timeout：定时阻塞监控时间，3种情况</span></span><br><span class="line"><span class="comment">//  1.NULL，永远等下去</span></span><br><span class="line"><span class="comment">//  2.设置timeval，等待固定时间</span></span><br><span class="line"><span class="comment">//  3.设置timeval里时间均为0，检查描述字后立即返回，轮询</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* </span></span><br><span class="line"><span class="comment">* select服务端伪码</span></span><br><span class="line"><span class="comment">* 首先一个线程不断接受客户端连接，并把socket文件描述符放到一个list里。</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">while</span>(<span class="number">1</span>) &#123;</span><br><span class="line">  connfd = <span class="built_in">accept</span>(listenfd);</span><br><span class="line">  <span class="built_in">fcntl</span>(connfd, F_SETFL, O_NONBLOCK);</span><br><span class="line">  fdlist.<span class="built_in">add</span>(connfd);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">* select函数还是返回刚刚提交的list，应用程序依然list所有的fd，只不过操作系统会将准备就绪的文件描述符做上标识，</span></span><br><span class="line"><span class="comment">* 用户层将不会再有无意义的系统调用开销。</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">timeval</span> timeout;</span><br><span class="line"><span class="type">int</span> max = <span class="number">0</span>;  <span class="comment">// 用于记录最大的fd，在轮询中时刻更新即可</span></span><br><span class="line"><span class="comment">// 初始化比特位</span></span><br><span class="line"><span class="built_in">FD_ZERO</span>(&amp;read_fd);</span><br><span class="line"><span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">    <span class="comment">// 阻塞获取 每次需要把fd从用户态拷贝到内核态</span></span><br><span class="line">    nfds = <span class="built_in">select</span>(max + <span class="number">1</span>, &amp;read_fd, &amp;write_fd, <span class="literal">NULL</span>, &amp;timeout);</span><br><span class="line">    <span class="comment">// 每次需要遍历所有fd，判断有无读写事件发生</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt;= max &amp;&amp; nfds; ++i) &#123;</span><br><span class="line">        <span class="comment">// 只读已就绪的文件描述符，不用过多遍历</span></span><br><span class="line">        <span class="keyword">if</span> (i == listenfd) &#123;</span><br><span class="line">            <span class="comment">// 这里处理accept事件</span></span><br><span class="line">            <span class="built_in">FD_SET</span>(i, &amp;read_fd);<span class="comment">//将客户端socket加入到集合中</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">FD_ISSET</span>(i, &amp;read_fd)) &#123;</span><br><span class="line">            <span class="comment">// 这里处理read事件</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>下面的动图能更直观的让我们了解select：</p><p><img src="https://s2.loli.net/2024/03/18/OGux9rQpCl3yiat.gif" alt="select.gif"></p><p>通过上面的select逻辑过程分析，相信大家都意识到，select存在三个问题：</p><blockquote><p>[1] 每次调用select，都需要把被监控的fds集合从用户态空间拷贝到内核态空间，高并发场景下这样的拷贝会使得消耗的资源是很大的。<br>[2] 能监听端口的数量有限，单个进程所能打开的最大连接数由FD_SETSIZE宏定义，监听上限就等于fds_bits位数组中所有元素的二进制位总数，其大小是32个整数的大小（在32位的机器上，大小就是3232，同理64位机器上为3264），当然我们可以对宏FD_SETSIZE进行修改，然后重新编译内核，但是性能可能会受到影响，一般该数和系统内存关系很大，具体数目可以<code>cat /proc/sys/fs/file-max</code>察看。32位机默认1024个，64位默认2048。</p></blockquote><p><img src="https://pic3.zhimg.com/80/v2-e842f26b011cf5d7cf05cce6b7a8d6e6_720w.webp"></p><blockquote><p>[3] 被监控的fds集合中，只要有一个有数据可读，整个socket集合就会被遍历一次调用sk的poll函数收集可读事件：由于当初的需求是朴素，仅仅关心是否有数据可读这样一个事件，当事件通知来的时候，由于数据的到来是异步的，我们不知道事件来的时候，有多少个被监控的socket有数据可读了，于是，只能挨个遍历每个socket来收集可读事件了。</p></blockquote><h1 id="poll"><a href="#poll" class="headerlink" title="poll"></a>poll</h1><p>poll的实现和select非常相似，只是描述fd集合的方式不同。针对select遗留的三个问题中（问题(2)是fd限制问题，问题(1)和(3)则是性能问题），poll只是使用pollfd结构而不是select的fd_set结构，这就解决了select的问题(2)fds集合大小1024限制问题。但poll和select同样存在一个性能缺点就是包含大量文件描述符的数组被整体复制于用户态和内核的地址空间之间，而不论这些文件描述符是否就绪，它的开销随着文件描述符数量的增加而线性增大。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">pollfd</span> &#123;</span><br><span class="line">　　 <span class="type">int</span> fd;           <span class="comment">/*文件描述符*/</span></span><br><span class="line">　　 <span class="type">short</span> events;     <span class="comment">/*监控的事件*/</span></span><br><span class="line">　　 <span class="type">short</span> revents;    <span class="comment">/*监控事件中满足条件返回的事件*/</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">poll</span><span class="params">(<span class="keyword">struct</span> pollfd *fds, <span class="type">unsigned</span> <span class="type">long</span> nfds, <span class="type">int</span> timeout)</span></span>;   </span><br></pre></td></tr></table></figure><p>函数参数：</p><ul><li><p>fds：struct pollfd类型的数组, 存储了待检测的文件描述符，struct pollfd有三个成员：</p></li><li><p>fd：委托内核检测的文件描述符</p></li><li><p>events：委托内核检测的fd事件（输入、输出、错误），每一个事件有多个取值</p></li><li><p>revents：这是一个传出参数，数据由内核写入，存储内核检测之后的结果</p></li><li><p>nfds：描述的是数组 fds 的大小</p></li><li><p>timeout: 指定poll函数的阻塞时长</p></li><li><p>-1：一直阻塞，直到检测的集合中有就绪的IO事件，然后解除阻塞函数返回</p></li><li><p>0：不阻塞，不管检测集合中有没有已就绪的IO事件，函数马上返回</p></li><li><p>大于0：表示 poll 调用方等待指定的毫秒数后返回</p></li></ul><p>函数返回值：</p><ul><li>-1：失败</li><li>大于0：表示检测的集合中已就绪的文件描述符的总个数</li></ul><p>下面是poll的函数原型，poll改变了fds集合的描述方式，使用了pollfd结构而不是select的fd_set结构，使得poll支持的fds集合限制远大于select的1024。poll虽然解决了fds集合大小1024的限制问题，从实现来看。很明显它并没优化大量描述符数组被整体复制于用户态和内核态的地址空间之间，以及个别描述符就绪触发整体描述符集合的遍历的低效问题。poll随着监控的socket集合的增加性能线性下降，使得poll也并不适合用于大并发场景。</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line">poll服务端实现伪码：</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">pollfd</span> fds[POLL_LEN];</span><br><span class="line"><span class="type">unsigned</span> <span class="type">int</span> nfds=<span class="number">0</span>;</span><br><span class="line">fds[<span class="number">0</span>].fd=server_sockfd;</span><br><span class="line">fds[<span class="number">0</span>].events=POLLIN|POLLPRI;</span><br><span class="line">nfds++;</span><br><span class="line"><span class="keyword">while</span> &#123;</span><br><span class="line">    res=<span class="built_in">poll</span>(fds,nfds,<span class="number">-1</span>);</span><br><span class="line">    <span class="keyword">if</span>(fds[<span class="number">0</span>].revents&amp;(POLLIN|POLLPRI)) &#123;</span><br><span class="line">        <span class="comment">//执行accept并加入fds中，nfds++</span></span><br><span class="line">        <span class="keyword">if</span>(--res&lt;=<span class="number">0</span>) <span class="keyword">continue</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//循环之后的fds</span></span><br><span class="line">    <span class="keyword">if</span>(fds[i].revents&amp;(POLLIN|POLLERR )) &#123;</span><br><span class="line">        <span class="comment">//读操作或处理异常等</span></span><br><span class="line">        <span class="keyword">if</span>(--res&lt;=<span class="number">0</span>) <span class="keyword">continue</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="epoll"><a href="#epoll" class="headerlink" title="epoll"></a>epoll</h1><p>在linux的网络编程中，很长的时间都在使用select来做事件触发。在linux新的内核中，有了一种替换它的机制，就是epoll。相比于select，epoll最大的好处在于它不会随着监听fd数目的增长而降低效率。如前面我们所说，在内核中的select实现中，它是采用轮询来处理的，轮询的fd数目越多，自然耗时越多。并且，在linux&#x2F;posix_types.h头文件有这样的声明：<br>#define __FD_SETSIZE 1024<br>表示select最多同时监听1024个fd，当然，可以通过修改头文件再重编译内核来扩大这个数目，但这似乎并不治本。</p><p>创建一个epoll的句柄，size用来告诉内核这个监听的数目一共有多大。这个参数不同于select()中的第一个参数，给出最大监听的fd+1的值。需要注意的是，当创建好epoll句柄后，它就是会占用一个fd值，在linux下如果查看&#x2F;proc&#x2F;进程id&#x2F;fd&#x2F;，是能够看到这个fd的，所以在使用完epoll后，必须调用close()关闭，否则可能导致fd被耗尽。</p><p>epoll的接口非常简单，一共就三个函数：</p><ul><li>epoll_create：创建一个epoll句柄</li><li>epoll_ctl：向 epoll 对象中添加&#x2F;修改&#x2F;删除要管理的连接</li><li>epoll_wait：等待其管理的连接上的 IO 事件</li></ul><p><strong>epoll_create 函数</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">epoll_create</span><span class="params">(<span class="type">int</span> size)</span></span>;</span><br></pre></td></tr></table></figure><ul><li><strong>功能：</strong>该函数生成一个 epoll 专用的文件描述符。</li><li><strong>参数size:</strong> 用来告诉内核这个监听的数目一共有多大，参数 size 并不是限制了 epoll 所能监听的描述符最大个数，只是对内核初始分配内部数据结构的一个建议。自从 linux 2.6.8 之后，size 参数是被忽略的，也就是说可以填只有大于 0 的任意值。</li><li><strong>返回值：</strong>如果成功，返回poll 专用的文件描述符，否者失败，返回-1。</li></ul><p>epoll_create的源码实现：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">SYSCALL_DEFINE1</span>(epoll_create1, <span class="type">int</span>, flags)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">eventpoll</span> *ep = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//创建一个 eventpoll 对象</span></span><br><span class="line">    error = <span class="built_in">ep_alloc</span>(&amp;ep);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//struct eventpoll 的定义</span></span><br><span class="line"><span class="comment">// file：fs/eventpoll.c</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">eventpoll</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//sys_epoll_wait用到的等待队列</span></span><br><span class="line">    <span class="type">wait_queue_head_t</span> wq;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//接收就绪的描述符都会放到这里</span></span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">list_head</span> rdllist;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//每个epoll对象中都有一颗红黑树</span></span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">rb_root</span> rbr;</span><br><span class="line"></span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">static</span> <span class="type">int</span> <span class="title">ep_alloc</span><span class="params">(<span class="keyword">struct</span> eventpoll **pep)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">eventpoll</span> *ep;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//申请 epollevent 内存</span></span><br><span class="line">    ep = <span class="built_in">kzalloc</span>(<span class="built_in">sizeof</span>(*ep), GFP_KERNEL);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//初始化等待队列头</span></span><br><span class="line">    <span class="built_in">init_waitqueue_head</span>(&amp;ep-&gt;wq);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//初始化就绪列表</span></span><br><span class="line">    <span class="built_in">INIT_LIST_HEAD</span>(&amp;ep-&gt;rdllist);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//初始化红黑树指针</span></span><br><span class="line">    ep-&gt;rbr = RB_ROOT;</span><br><span class="line"></span><br><span class="line">    ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>其中eventpoll 这个结构体中的几个成员的含义如下：</p><ul><li><strong>wq：</strong> 等待队列链表。软中断数据就绪的时候会通过 wq 来找到阻塞在 epoll 对象上的用户进程。</li><li><strong>rbr：</strong> 红黑树。为了支持对海量连接的高效查找、插入和删除，eventpoll 内部使用的就是红黑树。通过红黑树来管理用户主进程accept添加进来的所有 socket 连接。</li><li><strong>rdllist：</strong> 就绪的描述符链表。当有连接就绪的时候，内核会把就绪的连接放到 rdllist 链表里。这样应用进程只需要判断链表就能找出就绪进程，而不用去遍历红黑树的所有节点了。</li></ul><p><strong>epoll_ctl 函数</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">epoll_ctl</span><span class="params">(<span class="type">int</span> epfd, <span class="type">int</span> op, <span class="type">int</span> fd, <span class="keyword">struct</span> epoll_event *event)</span></span>; </span><br></pre></td></tr></table></figure><ul><li><strong>功能：</strong>epoll 的事件注册函数，它不同于 select() 是在监听事件时告诉内核要监听什么类型的事件，而是在这里先注册要监听的事件类型。</li><li><strong>参数epfd:</strong> epoll 专用的文件描述符，epoll_create()的返回值</li><li><strong>参数op:</strong> 表示动作，用三个宏来表示：</li></ul><ol><li>EPOLL_CTL_ADD：注册新的 fd 到 epfd 中；</li><li>EPOLL_CTL_MOD：修改已经注册的fd的监听事件；</li><li>EPOLL_CTL_DEL：从 epfd 中删除一个 fd；</li></ol><ul><li><strong>参数fd:</strong> 需要监听的文件描述符</li><li><strong>参数event:</strong> 告诉内核要监听什么事件，struct epoll_event 结构如:</li><li><strong>events****可以是以下几个宏的集合：</strong></li><li>EPOLLIN ：表示对应的文件描述符可以读（包括对端 SOCKET 正常关闭）；</li><li>EPOLLOUT：表示对应的文件描述符可以写；</li><li>EPOLLPRI：表示对应的文件描述符有紧急的数据可读（这里应该表示有带外数据到来）；</li><li>EPOLLERR：表示对应的文件描述符发生错误；</li><li>EPOLLHUP：表示对应的文件描述符被挂断；</li><li>EPOLLET ：将 EPOLL 设为边缘触发(Edge Trigger)模式，这是相对于水平触发(Level Trigger)来说的。</li><li>EPOLLONESHOT：只监听一次事件，当监听完这次事件之后，如果还需要继续监听这个 socket 的话，需要再次把这个 socket 加入到 EPOLL 队列里</li><li><strong>返回值：</strong>0表示成功，-1表示失败。</li></ul><p><strong>epoll_wait函数</strong></p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">epoll_wait</span><span class="params">(<span class="type">int</span> epfd, <span class="keyword">struct</span> epoll_event * events, <span class="type">int</span> maxevents, <span class="type">int</span> timeout)</span></span>; </span><br></pre></td></tr></table></figure><ul><li><strong>功能：</strong>等待事件的产生，收集在 epoll 监控的事件中已经发送的事件，类似于 select() 调用。</li><li><strong>参数epfd:</strong> epoll 专用的文件描述符，epoll_create()的返回值</li><li><strong>参数events:</strong> 分配好的 epoll_event 结构体数组，epoll 将会把发生的事件赋值到events 数组中（events 不可以是空指针，内核只负责把数据复制到这个 events 数组中，不会去帮助我们在用户态中分配内存）。</li><li><strong>参数maxevents:</strong> maxevents 告之内核这个 events 有多少个 。</li><li><strong>参数timeout:</strong> 超时时间，单位为毫秒，为 -1 时，函数为阻塞。</li><li><strong>返回值：</strong></li></ul><ol><li>如果成功，表示返回需要处理的事件数目</li><li>如果返回0，表示已超时</li><li>如果返回-1，表示失败</li></ol><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/socket.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;netinet/in.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;arpa/inet.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;errno.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;fcntl.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;cassert&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/epoll.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/types.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;sys/stat.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="type">const</span> <span class="type">int</span> MAX_EVENT_NUMBER = <span class="number">10000</span>; <span class="comment">//最大事件数</span></span><br><span class="line"><span class="comment">// 设置句柄非阻塞</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">setnonblocking</span><span class="params">(<span class="type">int</span> fd)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> old_option = <span class="built_in">fcntl</span>(fd, F_GETFL);</span><br><span class="line">    <span class="type">int</span> new_option = old_option | O_NONBLOCK;</span><br><span class="line">    <span class="built_in">fcntl</span>(fd, F_SETFL, new_option);</span><br><span class="line">    <span class="keyword">return</span> old_option;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建套接字</span></span><br><span class="line">    <span class="type">int</span> nRet=<span class="number">0</span>;</span><br><span class="line">    <span class="type">int</span> m_listenfd = <span class="built_in">socket</span>(PF_INET, SOCK_STREAM, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span>(m_listenfd&lt;<span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;fail to socket!&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// </span></span><br><span class="line">    <span class="keyword">struct</span> <span class="title class_">sockaddr_in</span> address;</span><br><span class="line">    <span class="built_in">bzero</span>(&amp;address, <span class="built_in">sizeof</span>(address));</span><br><span class="line">    address.sin_family = AF_INET;</span><br><span class="line">    address.sin_addr.s_addr = <span class="built_in">htonl</span>(INADDR_ANY);</span><br><span class="line">    address.sin_port = <span class="built_in">htons</span>(<span class="number">6666</span>);</span><br><span class="line"></span><br><span class="line">    <span class="type">int</span> flag = <span class="number">1</span>;</span><br><span class="line">    <span class="comment">// 设置ip可重用</span></span><br><span class="line">    <span class="built_in">setsockopt</span>(m_listenfd, SOL_SOCKET, SO_REUSEADDR, &amp;flag, <span class="built_in">sizeof</span>(flag));</span><br><span class="line">    <span class="comment">// 绑定端口号</span></span><br><span class="line">    <span class="type">int</span> ret = <span class="built_in">bind</span>(m_listenfd, (<span class="keyword">struct</span> sockaddr *)&amp;address, <span class="built_in">sizeof</span>(address));</span><br><span class="line">    <span class="keyword">if</span>(ret&lt;<span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;fail to bind!,errno :%d&quot;</span>,errno);</span><br><span class="line">        <span class="keyword">return</span> ret;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 监听连接fd</span></span><br><span class="line">    ret = <span class="built_in">listen</span>(m_listenfd, <span class="number">200</span>);</span><br><span class="line">    <span class="keyword">if</span>(ret&lt;<span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;fail to listen!,errno :%d&quot;</span>,errno);</span><br><span class="line">        <span class="keyword">return</span> ret;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 初始化红黑树和事件链表结构rdlist结构</span></span><br><span class="line">    epoll_event events[MAX_EVENT_NUMBER];</span><br><span class="line">    <span class="comment">// 创建epoll实例</span></span><br><span class="line">    <span class="type">int</span> m_epollfd = <span class="built_in">epoll_create</span>(<span class="number">5</span>);</span><br><span class="line">    <span class="keyword">if</span>(m_epollfd==<span class="number">-1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;fail to epoll create!&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> m_epollfd;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建节点结构体将监听连接句柄</span></span><br><span class="line">    epoll_event event;</span><br><span class="line">    event.data.fd = m_listenfd;</span><br><span class="line">    <span class="comment">//设置该句柄为边缘触发（数据没处理完后续不会再触发事件，水平触发是不管数据有没有触发都返回事件），</span></span><br><span class="line">    event.events = EPOLLIN | EPOLLET | EPOLLRDHUP;</span><br><span class="line">    <span class="comment">// 添加监听连接句柄作为初始节点进入红黑树结构中，该节点后续处理连接的句柄</span></span><br><span class="line">    <span class="built_in">epoll_ctl</span>(m_epollfd, EPOLL_CTL_ADD, m_listenfd, &amp;event);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//进入服务器循环</span></span><br><span class="line">    <span class="keyword">while</span>(<span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> number = <span class="built_in">epoll_wait</span>(m_epollfd, events, MAX_EVENT_NUMBER, <span class="number">-1</span>);</span><br><span class="line">        <span class="keyword">if</span> (number &lt; <span class="number">0</span> &amp;&amp; errno != EINTR)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">printf</span>( <span class="string">&quot;epoll failure&quot;</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; number; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="type">int</span> sockfd = events[i].data.fd;</span><br><span class="line">            <span class="comment">// 属于处理新到的客户连接</span></span><br><span class="line">            <span class="keyword">if</span> (sockfd == m_listenfd)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">struct</span> <span class="title class_">sockaddr_in</span> client_address;</span><br><span class="line">                <span class="type">socklen_t</span> client_addrlength = <span class="built_in">sizeof</span>(client_address);</span><br><span class="line">                <span class="type">int</span> connfd = <span class="built_in">accept</span>(m_listenfd, (<span class="keyword">struct</span> sockaddr *)&amp;client_address, &amp;client_addrlength);</span><br><span class="line">                <span class="keyword">if</span> (connfd &lt; <span class="number">0</span>)</span><br><span class="line">                &#123;</span><br><span class="line">                    <span class="built_in">printf</span>(<span class="string">&quot;errno is:%d accept error&quot;</span>, errno);</span><br><span class="line">                    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                epoll_event event;</span><br><span class="line">                event.data.fd = connfd;</span><br><span class="line">                <span class="comment">//设置该句柄为边缘触发（数据没处理完后续不会再触发事件，水平触发是不管数据有没有触发都返回事件），</span></span><br><span class="line">                event.events = EPOLLIN | EPOLLRDHUP;</span><br><span class="line">                <span class="comment">// 添加监听连接句柄作为初始节点进入红黑树结构中，该节点后续处理连接的句柄</span></span><br><span class="line">                <span class="built_in">epoll_ctl</span>(m_epollfd, EPOLL_CTL_ADD, connfd, &amp;event);</span><br><span class="line">                <span class="built_in">setnonblocking</span>(connfd);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (events[i].events &amp; (EPOLLRDHUP | EPOLLHUP | EPOLLERR))</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">//服务器端关闭连接，</span></span><br><span class="line">                <span class="built_in">epoll_ctl</span>(m_epollfd, EPOLL_CTL_DEL, sockfd, <span class="number">0</span>);</span><br><span class="line">                <span class="built_in">close</span>(sockfd);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">//处理客户连接上接收到的数据</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (events[i].events &amp; EPOLLIN)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="type">char</span> buf[<span class="number">1024</span>]=&#123;<span class="number">0</span>&#125;;</span><br><span class="line">                <span class="built_in">read</span>(sockfd,buf,<span class="number">1024</span>);</span><br><span class="line">                <span class="built_in">printf</span>(<span class="string">&quot;from client :%s&quot;</span>);</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 将事件设置为写事件返回数据给客户端</span></span><br><span class="line">                events[i].data.fd = sockfd;</span><br><span class="line">                events[i].events = EPOLLOUT | EPOLLET | EPOLLONESHOT | EPOLLRDHUP;</span><br><span class="line">                <span class="built_in">epoll_ctl</span>(m_epollfd, EPOLL_CTL_MOD, sockfd, &amp;events[i]);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (events[i].events &amp; EPOLLOUT)</span><br><span class="line">            &#123;</span><br><span class="line">                std::string response = <span class="string">&quot;server response \n&quot;</span>;</span><br><span class="line">                <span class="built_in">write</span>(sockfd,response.<span class="built_in">c_str</span>(),response.<span class="built_in">length</span>());</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 将事件设置为读事件，继续监听客户端</span></span><br><span class="line">                events[i].data.fd = sockfd;</span><br><span class="line">                events[i].events = EPOLLIN | EPOLLRDHUP;</span><br><span class="line">                <span class="built_in">epoll_ctl</span>(m_epollfd, EPOLL_CTL_MOD, sockfd, &amp;events[i]);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">//else if 可以加管道，unix套接字等等数据</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><img src="https://s2.loli.net/2024/03/18/SlaDPRfpKqFU4kr.gif" alt="poll.gif"></p><h2 id="epoll的边缘触发与水平触发"><a href="#epoll的边缘触发与水平触发" class="headerlink" title="epoll的边缘触发与水平触发"></a>epoll的边缘触发与水平触发</h2><p><strong>水平触发(LT)</strong></p><p>关注点是数据是否有无，只要读缓冲区不为空，写缓冲区不满，那么epoll_wait就会一直返回就绪，水平触发是epoll的默认工作方式。</p><p><strong>边缘触发(ET)</strong></p><p>关注点是变化，只要缓冲区的数据有变化，epoll_wait就会返回就绪。<br>这里的数据变化并不单纯指缓冲区从有数据变为没有数据，或者从没有数据变为有数据，还包括了数据变多或者变少。即当buffer长度有变化时，就会触发。<br>假设epoll被设置为了边缘触发，当客户端写入了100个字符，由于缓冲区从0变为了100，于是服务端epoll_wait触发一次就绪，服务端读取了2个字节后不再读取。这个时候再去调用epoll_wait会发现不会就绪，只有当客户端再次写入数据后，才会触发就绪。<br>这就导致如果使用ET模式，那就必须保证要「一次性把数据读取&amp;写入完」，否则会导致数据长期无法读取&#x2F;写入。</p><h2 id="epoll-为什么比select、poll更高效？"><a href="#epoll-为什么比select、poll更高效？" class="headerlink" title="epoll 为什么比select、poll更高效？"></a><strong>epoll 为什么比select、poll更高效？</strong></h2><ul><li>epoll 采用红黑树管理文件描述符<br>  从上图可以看出，epoll使用红黑树管理文件描述符，红黑树插入和删除的都是时间复杂度 O(logN)，不会随着文件描述符数量增加而改变。<br>  select、poll采用数组或者链表的形式管理文件描述符，那么在遍历文件描述符时，时间复杂度会随着文件描述的增加而增加。</li><li>epoll 将文件描述符添加和检测分离，减少了文件描述符拷贝的消耗<br>  select&amp;poll 调用时会将全部监听的 fd 从用户态空间拷贝至内核态空间并线性扫描一遍找出就绪的 fd 再返回到用户态。下次需要监听时，又需要把之前已经传递过的文件描述符再读传递进去，增加了拷贝文件的无效消耗，当文件描述很多时，性能瓶颈更加明显。<br>  而epoll只需要使用epoll_ctl添加一次，后续的检查使用epoll_wait，减少了文件拷贝的消耗。</li></ul><h1 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h1><p>select，poll，epoll都是IO多路复用机制，即可以监视多个描述符，一旦某个描述符就绪（读或写就绪），能够通知程序进行相应读写操作。 但select，poll，epoll本质上都是<strong>同步I&#x2F;O</strong>，因为他们都需要在读写事件就绪后自己负责进行读写，也就是说这个读写过程是阻塞的，而异步I&#x2F;O则无需自己负责进行读写，异步I&#x2F;O的实现会负责把数据从内核拷贝到用户空间。</p><ul><li>select，poll实现需要自己不断轮询所有fd集合，直到设备就绪，期间可能要睡眠和唤醒多次交替。而epoll其实也需要调用epoll_wait不断轮询就绪链表，期间也可能多次睡眠和唤醒交替，但是它是设备就绪时，调用回调函数，把就绪fd放入就绪链表中，并唤醒在epoll_wait中进入睡眠的进程。虽然都要睡眠和交替，但是select和poll在“醒着”的时候要遍历整个fd集合，而epoll在“醒着”的时候只要判断一下就绪链表是否为空就行了，这节省了大量的CPU时间。这就是回调机制带来的性能提升。</li><li>select，poll每次调用都要把fd集合从用户态往内核态拷贝一次，并且要把current往设备等待队列中挂一次，而epoll只要一次拷贝，而且把current往等待队列上挂也只挂一次（在epoll_wait的开始，注意这里的等待队列并不是设备等待队列，只是一个epoll内部定义的等待队列）。这也能节省不少的开销。</li></ul><table><thead><tr><th></th><th>select</th><th>poll</th><th>epoll</th></tr></thead><tbody><tr><td>性能</td><td>随着连接数的增加，性能急剧下降，处理成千上万的并发连接数时，性能很差</td><td>随着连接数的增加，性能急剧下降，处理成千上万的并发连接数时，性能很差</td><td>随着连接数的增加，性能基本没有变化</td></tr><tr><td>连接数</td><td>一般1024</td><td>无限制</td><td>无限制</td></tr><tr><td>内存拷贝</td><td>每次调用select拷贝</td><td>每次调用poll拷贝</td><td>fd首次调用epoll_ctl拷贝，每次调用epoll_wait不拷贝</td></tr><tr><td>数据结构</td><td>bitmap</td><td>数组</td><td>红黑树</td></tr><tr><td>内在处理机制</td><td>线性轮询</td><td>线性轮询</td><td>FD挂在红黑树，通过事件回调callback</td></tr><tr><td>时间复杂度</td><td>O(n)</td><td>O(n)</td><td>O(1)</td></tr></tbody></table><hr><p>转载：<a href="https://zhuanlan.zhihu.com/p/367591714">IO多路复用——深入浅出理解select、poll、epoll的实现 - 知乎 (zhihu.com)</a></p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>Hello World</title>
      <link href="/2024/03/17/hello-world/"/>
      <url>/2024/03/17/hello-world/</url>
      
        <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>编程学习</title>
      <link href="/2024/03/17/%E5%9F%BA%E7%A1%80%E9%97%AE%E9%A2%98/"/>
      <url>/2024/03/17/%E5%9F%BA%E7%A1%80%E9%97%AE%E9%A2%98/</url>
      
        <content type="html"><![CDATA[<h1 id="一-C"><a href="#一-C" class="headerlink" title="一. C++"></a>一. C++</h1><h2 id="1-回调函数"><a href="#1-回调函数" class="headerlink" title="1. 回调函数"></a>1. 回调函数</h2><p>C++回调函数是一种可以在程序执行时动态绑定，被其他函数调用的函数。它通过将一个函数指针传递给另一个函数，使得后者可以在需要时直接调用前者。</p><p>通常情况下，回调函数包括两个部分：回调函数本身和被调函数。被调函数在执行过程中遇到某些条件会执行回调函数，并将参数传递给回调函数，以便回调函数进行进一步的处理。</p><p>C++回调函数经常在通信模块中使用，因为通信模块需要与其他模块进行交互，并根据相应的事件和数据执行相应的操作。例如，在网络编程中，当某个事件（如连接建立完成、数据接收完毕等）发生时，就需要执行相应的回调函数来处理数据或发送信号。在GUI开发中，也常常使用回调函数来响应用户界面上的操作，如鼠标点击、键盘输入等。</p><p>在通信模块中使用回调函数的好处在于，它可以使代码具有更好的灵活性和可重用性。通过使用回调函数，我们可以实现不同模块之间的解耦，使得不同的模块可以独立发展，减少了模块之间的耦合关系，并且可以更方便地进行单元测试和调试。</p><p>总之，C++回调函数是一种重要的编程技术，它可以使程序更加灵活，可复用性更高，在通信模块、GUI开发和其他需要动态绑定的场景中都有广泛的应用。</p><p>下面是一个简单的C++回调函数的示例代码：</p><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义回调函数类型</span></span><br><span class="line"><span class="function"><span class="keyword">typedef</span> <span class="title">void</span> <span class="params">(*CallbackFunc)</span><span class="params">(<span class="type">int</span>)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 被调函数，接收一个回调函数作为参数</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">doSomething</span><span class="params">(<span class="type">int</span> value, CallbackFunc callback)</span> </span>&#123;</span><br><span class="line">  std::cout &lt;&lt; <span class="string">&quot;doSomething: &quot;</span> &lt;&lt; value &lt;&lt; std::endl;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 执行回调函数</span></span><br><span class="line">  <span class="keyword">if</span> (callback != <span class="literal">nullptr</span>) &#123;</span><br><span class="line">    <span class="built_in">callback</span>(value + <span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 回调函数，处理被调函数传递过来的参数</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">onCallback</span><span class="params">(<span class="type">int</span> value)</span> </span>&#123;</span><br><span class="line">  std::cout &lt;&lt; <span class="string">&quot;onCallback: &quot;</span> &lt;&lt; value &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="comment">// 使用回调函数调用被调函数</span></span><br><span class="line">  <span class="built_in">doSomething</span>(<span class="number">10</span>, onCallback);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>以上代码中，我们定义了一个名为 <code>CallbackFunc</code> 的函数指针类型，它接收一个整数参数并返回空。然后我们编写了两个函数：<code>doSomething</code> 和 <code>onCallback</code>。其中，<code>doSomething</code> 是被调函数，它接受一个整数 <code>value</code> 和一个回调函数 <code>callback</code> 作为参数。<code>onCallback</code> 则是我们定义的回调函数，它接收一个整数 <code>value</code> 作为参数，并在控制台输出。</p><p>在主函数中，我们通过调用 <code>doSomething(10, onCallback)</code>，向 <code>doSomething</code> 函数传递了 <code>10</code> 这个整数和回调函数 <code>onCallback</code>。在 <code>doSomething</code> 中，当执行到回调函数的时候，会将传递进来的 <code>value</code> 参数加 1，并执行回调函数 <code>onCallback</code>。在 <code>onCallback</code> 中，我们再次输出了这个参数的值。</p><p>这个示例展示了如何使用 C++ 回调函数来实现程序中的动态绑定和事件响应操作。</p><h2 id="2-new对象和直接创建对象"><a href="#2-new对象和直接创建对象" class="headerlink" title="2. new对象和直接创建对象"></a>2. new对象和直接创建对象</h2><p>这两个构造方法的区别在于对象的创建方式和生命周期。</p><ol><li><code>recv_worker = new Worker(&quot;receiver&quot;);</code></li></ol><p>这行代码使用了动态内存分配，通过<code>new</code>运算符在堆上创建了一个名为<code>recv_worker</code>的<code>Worker</code>对象。这样创建的对象的生命周期由手动管理，需要<strong>手动调用<code>delete</code>来释放对应的内存空间</strong>，否则会导致内存泄漏。</p><ol start="2"><li><code>Worker main_work(&quot;main&quot;);</code></li></ol><p>这行代码通过直接创建一个名为<code>main_work</code>的<code>Worker</code>对象，不使用动态内存分配。这样创建的对象的生命周期由其所在的作用域管理，当超出该作用域时，对象会自动被销毁并释放相应的内存空间。</p><p>选择使用动态内存分配的方式（<code>new</code>运算符）通常是在以下情况下考虑：</p><ul><li>对象的生命周期需要跨越多个函数或作用域。</li><li>对象的大小较大，无法容纳在栈上。</li><li>需要在程序的其他地方继续访问对象。</li></ul><p>在使用动态内存分配时，需要注意手动释放分配的内存空间，以避免内存泄漏。但同时也增加了手动管理内存的复杂性。</p><p>使用直接创建对象的方式更简洁，并且在对象生命周期比较短暂且作用域有限的情况下更为常见和方便。</p><p>具体选择哪种方式构造对象取决于代码的需求、对象的生命周期以及内存管理的复杂性和性能考虑。</p><h2 id="3-bind函数"><a href="#3-bind函数" class="headerlink" title="3. bind函数"></a>3. bind函数</h2><p><code>std::bind</code> 是 C++ 标准库中的一个函数模板，用于创建函数对象（也称为绑定器），它可以绑定函数、成员函数、函数对象或者函数指针，并通过指定参数来修改绑定后的函数对象的行为。</p><p><code>std::bind</code> 的基本语法如下：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt; <span class="keyword">class</span> <span class="title class_">F</span>, <span class="keyword">class</span>... Args &gt;</span><br><span class="line"><span class="comment">/* 返回值类型 */</span> <span class="built_in">bind</span>( F&amp;&amp; f, Args&amp;&amp;... args );</span><br></pre></td></tr></table></figure><p>其中，<code>F</code> 表示要绑定的函数、成员函数、函数对象或函数指针的类型，<code>Args</code> 表示要传递给绑定的函数对象的参数类型。</p><p>使用 <code>std::bind</code> 可以解决以下问题：</p><ol><li><p>绑定普通函数：可以将函数及其参数进行绑定，创建一个函数对象。</p></li><li><p>绑定成员函数：可以将成员函数与特定对象实例进行绑定，创建一个函数对象。</p></li><li><p>参数占位符：可以使用占位符 <code>_n</code> 来表示尚未绑定的参数位置，之后可以在调用时提供这些参数。</p></li><li><p>参数传递和修改：可以在绑定时指定部分参数，绑定后的函数对象在调用时可以提供剩余的参数或者修改已绑定的参数。</p></li></ol><p>下面是一些常见用法的示例：</p><ol><li>绑定普通函数：</li></ol><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> boundFunc = std::<span class="built_in">bind</span>(add, <span class="number">5</span>, <span class="number">10</span>);</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">boundFunc</span>() &lt;&lt; std::endl;  <span class="comment">// 输出 15</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ol start="2"><li>绑定成员函数：</li></ol><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">multiply</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> a * b;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    MyClass obj;</span><br><span class="line">    <span class="keyword">auto</span> boundFunc = std::<span class="built_in">bind</span>(&amp;MyClass::multiply, &amp;obj, <span class="number">5</span>, <span class="number">10</span>);</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">boundFunc</span>() &lt;&lt; std::endl;  <span class="comment">// 输出 50</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ol start="3"><li>使用占位符和参数传递：</li></ol><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b, <span class="type">int</span> c)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b + c;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> boundFunc = std::<span class="built_in">bind</span>(add, std::placeholders::_1, <span class="number">10</span>, std::placeholders::_2);</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">boundFunc</span>(<span class="number">5</span>, <span class="number">15</span>) &lt;&lt; std::endl;  <span class="comment">// 输出 30</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>通过使用 <code>std::bind</code>，可以更灵活地操作函数对象的行为，包括参数绑定、参数占位符和参数传递。这使得代码更具可读性和重用性，并且在某些情况下可以提供更简洁的编程解决方案。</p><ol><li><p>消息队列：使用一个线程安全的队列数据结构作为消息队列，用于存储待处理的消息。</p></li><li><p>消息生产者：在应用程序的不同部分或模块中，有需要向消息队列发送消息的地方。这些消息可以是需要后台处理的任务、事件或其他通知。</p></li><li><p>消息消费者：一个或多个消费者线程从消息队列中获取消息，并进行处理。每个消费者线程从队列中获取一个消息，并在后台线程中处理或执行相关操作。</p></li><li><p>多线程处理：您的多线程框架可以用来创建和管理消费者线程。这些线程可以并行地处理消息，提高处理速度和吞吐量。</p></li><li><p>异步定时器：您的异步定时器可以用来进行消息的定期处理、延迟处理或重试操作。您可以设置定时器来触发特定的操作，例如定期检查队列中的消息、定时触发后台任务等。</p></li><li><p>错误处理与确认机制：在消息处理过程中，可能会发生错误或需要确认消息已被正确处理。您可以设计错误处理机制，例如记录错误日志或进行错误重试。同时，可以在消息队列中引入确认机制，确保消息被成功处理并从队列中移除。</p></li><li><p>监控和管理：为了监控系统运行情况和管理消息队列，您可以设计一些管理功能，例如查看当前队列长度、消费者线程状态、错误日志等。</p></li></ol><p>当用C++实现一个消息队列处理系统时，可以按照以下设计进行：</p><ol><li>消息队列：使用一个线程安全的队列数据结构来实现消息队列。可以使用标准库提供的<code>std::queue</code>或者第三方库（如Boost）中的线程安全队列。</li></ol><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;queue&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;mutex&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;condition_variable&gt;</span></span></span><br><span class="line"></span><br><span class="line">std::queue&lt;Message&gt; messageQueue;</span><br><span class="line">std::mutex mtx;</span><br><span class="line">std::condition_variable cv;</span><br></pre></td></tr></table></figure><ol start="2"><li>消息结构体：定义一个消息结构体，包含需要传递的信息。</li></ol><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Message</span> &#123;</span><br><span class="line">    <span class="comment">// 消息内容</span></span><br><span class="line">    std::string content;</span><br><span class="line">    <span class="comment">// 其他可能的字段</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><ol start="3"><li>消息生产者：在需要发送消息的地方，调用一个函数将消息推送到消息队列。</li></ol><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">pushMessage</span><span class="params">(<span class="type">const</span> Message&amp; message)</span> </span>&#123;</span><br><span class="line">    <span class="function">std::lock_guard&lt;std::mutex&gt; <span class="title">lock</span><span class="params">(mtx)</span></span>;</span><br><span class="line">    messageQueue.<span class="built_in">push</span>(message);</span><br><span class="line">    cv.<span class="built_in">notify_one</span>();  <span class="comment">// 通知一个等待的消费者线程</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ol start="4"><li>消息消费者：创建多个消费者线程，从消息队列中获取消息，并进行处理。</li></ol><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">consumerThread</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">        <span class="function">std::unique_lock&lt;std::mutex&gt; <span class="title">lock</span><span class="params">(mtx)</span></span>;</span><br><span class="line">        cv.<span class="built_in">wait</span>(lock, []&#123; <span class="keyword">return</span> !messageQueue.<span class="built_in">empty</span>(); &#125;);  <span class="comment">// 等待队列非空</span></span><br><span class="line">        Message message = messageQueue.<span class="built_in">front</span>();</span><br><span class="line">        messageQueue.<span class="built_in">pop</span>();</span><br><span class="line">        lock.<span class="built_in">unlock</span>();</span><br><span class="line"></span><br><span class="line">        <span class="built_in">processMessage</span>(message);  <span class="comment">// 处理消息</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ol start="5"><li>多线程处理：在主函数中创建并启动多个消费者线程。</li></ol><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::vector&lt;std::thread&gt; consumerThreads;</span><br><span class="line">    <span class="type">int</span> numThreads = <span class="number">4</span>; <span class="comment">// 设置消费者线程数量</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; numThreads; ++i) &#123;</span><br><span class="line">        consumerThreads.<span class="built_in">emplace_back</span>(consumerThread);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 进行其他操作</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span>&amp; thread : consumerThreads) &#123;</span><br><span class="line">        thread.<span class="built_in">join</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ol start="6"><li>错误处理与确认机制：根据需要，在<code>processMessage()</code>函数中添加错误处理逻辑，并确保消息被成功处理后从队列中移除。</li></ol><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">processMessage</span><span class="params">(<span class="type">const</span> Message&amp; message)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        <span class="comment">// 处理消息的逻辑</span></span><br><span class="line">    &#125; <span class="built_in">catch</span> (<span class="type">const</span> std::exception&amp; e) &#123;</span><br><span class="line">        <span class="comment">// 处理错误的逻辑，并记录错误日志</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ol start="7"><li>监控和管理：根据需求，可以添加相关功能，如查看队列长度的函数、查询消费者线程状态等。</li></ol><p>当你运行此代码时，它会执行以下步骤：</p><ol><li><p>创建 <code>oneshot_timer</code> 和 <code>re_timer</code> 对象，并将它们附加到 <code>send_worker</code> 工作线程上。</p></li><li><p>创建 <code>recv_worker</code> 接收线程，并启动该线程。这个线程将处理从 <code>send_worker</code> 发送过来的任务。</p></li><li><p>调用 <code>re_timer-&gt;start(5000)</code>，表示重复定时器 <code>re_timer</code> 将在5000毫秒（5秒）后开始触发，然后每隔5000毫秒再触发一次。</p></li><li><p>调用 <code>oneshot_timer-&gt;start(3000)</code>，表示一次性定时器 <code>oneshot_timer</code> 将在3000毫秒（3秒）后触发一次。</p></li><li><p><code>send_worker-&gt;start(WORKER_FOREGROUND)</code> 启动 <code>send_worker</code> 工作线程。</p></li><li><p>一旦定时器触发，就会调用相应的回调函数。</p><ul><li><p>当 <code>oneshot_timer</code> 触发时，将调用 <code>once_timer</code> 回调函数。在这个函数中，它会递增一个变量 <code>iter</code>，并使用 <code>iter * 10000</code> 的值重新启动 <code>oneshot_timer</code>。这样，每次 <code>oneshot_timer</code> 触发时，都会以递增的时间间隔重新启动。</p></li><li><p>当 <code>re_timer</code> 触发时，将调用 <code>repeat_timer</code> 回调函数。在这个函数中，它会创建一个 <code>Job</code> 对象，并根据 <code>sync</code> 变量的值选择以同步或异步方式将任务提交给接收线程。如果 <code>sync</code> 为真，则使用 <code>recv_worker-&gt;submitSync</code> 方法提交任务，否则使用 <code>recv_worker-&gt;submitAsync</code> 方法提交任务。</p></li></ul></li><li><p>在 <code>repeat_timer</code> 回调函数中，当 <code>sync</code> 为真时，还创建了一个 <code>TestJob</code> 对象，并将 <code>testa</code> 的值传递给它。然后，通过 <code>recv_worker-&gt;submitSync</code> 方法将 <code>TestJob</code> 提交到接收线程。最后，打印出 <code>testa</code> 的值。</p></li></ol><p>总结起来，这段代码创建了两个定时器（一个一次性定时器和一个重复定时器），并设置了它们的触发时间。定时器触发时，会调用相应的回调函数，在回调函数中执行特定的操作，如重新启动定时器、创建任务并将其提交给接收线程等。</p><p>希望这解释了代码的逻辑。如果还有其他问题，请随时提问。</p><h2 id="4-子类重写父类虚函数"><a href="#4-子类重写父类虚函数" class="headerlink" title="4. 子类重写父类虚函数"></a>4. 子类重写父类虚函数</h2><p>在C++中，一个类可以从另一个类继承，这个被继承的类称为基类（或父类），而继承这个基类的类称为派生类（或子类）。当子类从父类继承时，它可以访问所有父类公共和保护的成员。如果需要在子类中修改或扩展父类的行为，就可以重写父类的虚函数。</p><p>重写父类虚函数可以提供更多的灵活性和可扩展性。为了重写父类的虚函数，需要遵循以下步骤：</p><ol><li>创建一个与父类虚函数声明相同的函数，在子类中声明该函数为 <code>virtual</code> 类型，并使用 <code>override</code> 关键字标记该函数。</li></ol><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Parent</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">foo</span><span class="params">()</span> </span>&#123;&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Child</span> : <span class="keyword">public</span> Parent &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">foo</span><span class="params">()</span> <span class="keyword">override</span> </span>&#123;&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><ol start="2"><li>在子类中重新实现该函数的功能。</li></ol><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Parent</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">foo</span><span class="params">()</span> </span>&#123;&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Child</span> : <span class="keyword">public</span> Parent &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">foo</span><span class="params">()</span> <span class="keyword">override</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 在这里实现子类需要的新功能</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><ol start="3"><li>如果需要，在子类中可以通过调用父类的虚函数来扩展或修改父类函数的行为。</li></ol><figure class="highlight c++"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Parent</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">foo</span><span class="params">()</span> </span>&#123;&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Child</span> : <span class="keyword">public</span> Parent &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">foo</span><span class="params">()</span> <span class="keyword">override</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 执行父类的行为</span></span><br><span class="line">        Parent::<span class="built_in">foo</span>();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 在这里添加子类需要的新功能</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>这就是在C++中重写父类虚函数的基本步骤。通过这种方式，你可以实现在子类中扩展和修改父类的行为，从而提供更灵活、可扩展的代码结构。</p><h2 id="5-全局变量和静态变量"><a href="#5-全局变量和静态变量" class="headerlink" title="5. 全局变量和静态变量"></a>5. 全局变量和静态变量</h2><p>全局变量和静态变量在内存中存放在同一位置，即静态区&#x2F;全局区。</p><p>静态区&#x2F;全局区分为两个部分，一部分存放已初始化的变量，另一部分存储未初始化的变量</p><p>对基本类型的静态变量若在声明时未赋以初值，则系统自动赋予0值。</p><p>从作用域来看：</p><p>静态变量的生命周期为整个源程序，其作用域仍与普通变量相同</p><p>静态全局变量也具有全局作用域，他与全局变量的区别在于如果程序包含多个文件的话，他作用于定义它的文件里，不能作用到其他文件里，即被static关键字修饰过的变量具有文件作用域。这样即使两个不同的源文件都定义了相同的静态全局变量，他们也是不同的变量</p><p>从分配内存空间看：</p><p>全局变量、静态局部变量、静态全局变量都在静态存储区分配空间，而局部变量在栈分配空间。</p><p>从生命周期看： 都在程序运行时期全程有效</p><h2 id="左移-右移运算符"><a href="#左移-右移运算符" class="headerlink" title="左移&#x2F;右移运算符"></a>左移&#x2F;右移运算符</h2><p>当我们进行右移运算时，将一个二进制数向右移动指定位数的位置。举个例子来说明：</p><p>假设我们有一个无符号8位整数<code>a</code>，其二进制表示为 <code>10111010</code>，我们将其向右移动2位。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">   a: 10111010</span><br><span class="line">&gt;&gt; 2</span><br></pre></td></tr></table></figure><p>右移2位后，最右边的两位将被舍弃，而左侧的位则向右移动填补。</p><p>结果是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">00101110</span><br></pre></td></tr></table></figure><p>这个新的二进制数对应的十进制值为46。</p><p>在右移运算中，被移出的位会被丢弃，而新增的位会用0来填充。如果进行的是算术右移（对于有符号整数），则符号位会保持不变，即用符号位填充新增的位，以保持数值的符号。</p><p>例如，在进行算术右移时，如果我们有一个有符号8位整数<code>b</code>，其二进制表示为 <code>11100101</code>，我们将其向右移动3位。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">   b: 11100101</span><br><span class="line">&gt;&gt; 3</span><br></pre></td></tr></table></figure><p>由于是算术右移，符号位为1，因此新增的位将使用1来填充。</p><p>结果是：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">11111100</span><br></pre></td></tr></table></figure><p>这个新的二进制数对应的十进制值为-4。</p><p>总结一下，右移运算是将二进制数向右移动指定的位数，并用0填充新增的位（对于无符号整数），或者用符号位填充新增的位（对于有符号整数）。</p><h2 id="QNX栈内存分配"><a href="#QNX栈内存分配" class="headerlink" title="QNX栈内存分配"></a>QNX栈内存分配</h2><h2 id="extern-“C”"><a href="#extern-“C”" class="headerlink" title="extern “C”"></a>extern “C”</h2><h3 id="一-extern关键字"><a href="#一-extern关键字" class="headerlink" title="一. extern关键字"></a>一. <a href="https://so.csdn.net/so/search?q=extern&spm=1001.2101.3001.7020">extern</a>关键字</h3><p><code>extern</code> 关键字的用法很简单，就是简简单单的声明，它可以明确的指出一个语句是声明；<br>比如<code>extern int i</code>;那么就说明 这是声明变量 i,而不是定义i,<strong>声明是不开辟内存的，定义是开辟内存的</strong>。<br>假如<code>int i</code>；没有<code>extern</code>修饰，那就说明为定义，会分配内存空间的。</p><h4 id="extern-的使用场景"><a href="#extern-的使用场景" class="headerlink" title="extern 的使用场景"></a>extern 的使用场景</h4><p>假如我一个大工程，这个工程由超级多的文件，这些文件假如都要访问<br>test.c文件的 变量 i,那么，只能在这些文件中，每个文件的开头都 声明变量 i,并且，假如我的test.c，不止一个定义一个变量i,有好多其他变量呢？在其他文件访问时候，都要声明好多变量，这会使得书写难度很繁琐，并且维护成本也大；</p><p>所以一般，我们都把声明语句放到头文件使用，即我定义一个test.h的头文件；<br>在该头文件中，声明 extern int i；<br>然后，假如你在其他文件要使用改变量i,直接包含该头文件即可，<br>比如：test.h 头文件</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//test.h 头文件</span></span><br><span class="line"><span class="keyword">extern</span> <span class="type">int</span> i;</span><br><span class="line"><span class="keyword">extern</span> <span class="type">int</span> j;</span><br><span class="line"><span class="keyword">extern</span> <span class="type">int</span> k;</span><br><span class="line"><span class="comment">//...</span></span><br><span class="line"><span class="comment">//声明很多很多变量</span></span><br></pre></td></tr></table></figure><p>在其他.c文件，只要包含该头文件，就可以啦，比如在<code>main.c</code>文件：</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta"># <span class="keyword">include</span><span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta"># <span class="keyword">include</span><span class="string">&quot;test.h&quot;</span></span></span><br><span class="line"><span class="comment">//extern int i; 不用写了</span></span><br><span class="line"><span class="comment">//extern int j;不用写了</span></span><br><span class="line"><span class="comment">//extern int k;不用写了</span></span><br><span class="line"><span class="comment">//...</span></span><br><span class="line"><span class="comment">//声明很多很多变量,都不用写了，因为包含了头文件，声明都在头文件中</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%d %d %d&quot;</span>,i,j,k);</span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h4 id="extern的用法总结"><a href="#extern的用法总结" class="headerlink" title="extern的用法总结"></a>extern的用法总结</h4><p>extern一般用于声明，在.h文件中，声明变量或者函数（函数可以不加extern，但是最好加上，这样比较统一）；在其他文件要访问该变量函数时候，包含头文件就行哦。</p><h3 id="二-extern-“C”-的理解和用法"><a href="#二-extern-“C”-的理解和用法" class="headerlink" title="二. extern “C” 的理解和用法"></a>二. extern “C” 的理解和用法</h3><p>在C++中，extern”C”修饰的语句是按照C语言的方式进行编译的。</p><p>怎么理解这句话呢？</p><p>我来举几个例子：（理解它extern”C”的含义）<br>我们知道在C++中函数是可以发生重载的，即编译的时候并不会报错,在C语言中，是没有重载的说法的;那么假如我用extern”C”去修饰重载的函数的话，即在编译时候，就会按照C语言的方式去编译了：<br>这个时候，就会发生错误；<br>看例子：创建一个main.cpp文件</p><figure class="highlight cpp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta"># <span class="keyword">include</span><span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">()</span> <span class="comment">//用 extern&quot;C&quot;修饰</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">extern</span> <span class="string">&quot;C&quot;</span> <span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(<span class="type">int</span> v)</span><span class="comment">//用 extern&quot;C&quot;修饰</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">&#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 编程 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 原创 </tag>
            
            <tag> 编程 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>统一诊断服务（UDS）</title>
      <link href="/2024/03/17/%E7%BB%9F%E4%B8%80%E8%AF%8A%E6%96%AD%E6%9C%8D%E5%8A%A1%EF%BC%88UDS%EF%BC%89/"/>
      <url>/2024/03/17/%E7%BB%9F%E4%B8%80%E8%AF%8A%E6%96%AD%E6%9C%8D%E5%8A%A1%EF%BC%88UDS%EF%BC%89/</url>
      
        <content type="html"><![CDATA[<p>诊断服务常用于诊断仪（客户端）和<a href="https://so.csdn.net/so/search?q=ECU&spm=1001.2101.3001.7020">ECU</a>（服务器）之间的会话控制、安全访问、例程控制、DTC读取，ECU软件刷写（软件下载）等</p><p><img src="https://s2.loli.net/2024/03/17/jaImN1PkDbxwR7L.png" alt="Pasted image 20230919090838.png"><br>![[Pasted image 20230919090838.png]]</p><p>![[Pasted image 20230919132051.png]]</p><p>![[Pasted image 20230919094110.png]]</p><h1 id="一-诊断术语"><a href="#一-诊断术语" class="headerlink" title="一. 诊断术语"></a>一. 诊断术语</h1><p><strong>Diagnostic Service （诊断服务）</strong></p><blockquote><p>诊断服务是介于诊断设备和ECU之间的一种信息交互方式。通常由诊断设备发出请求，ECU做出回应。</p></blockquote><p><strong>Diagnostic Trouble Code （故障码）</strong></p><blockquote><p>故障码是用来标记ECU故障的代码，它遵循一定的规则，存储在ECU的非易失性存储中。</p></blockquote><p><strong>Diagnostic Data （诊断数据）</strong></p><blockquote><p>诊断数据是可以被诊断设备请求的ECU内部数据，它包括：<br>    - 当前数据：ECU正在运行的数据，比如车速、节气门开度，发动机转速等；<br>    - 存储数据：被ECU存储在存储器中某时刻的数据，比如DTC；<br>    - 静态数据：恒定不变的ECU内部数据，比如VIN码。</p></blockquote><p><strong>Diagnostic Session （诊断会话）</strong></p><blockquote><p>可以理解为某种诊断模式或权限，即在不同的模式下，对不同的诊断服务的使用做了限制。</p></blockquote><p><strong>Diagnostic Routine （诊断例程）</strong></p><blockquote><p>驻留在被诊断ECU中的子程序，它可以被诊断设备启动和停止。比如格式化EEPROM的子程序。</p></blockquote><p><strong>Addressing Type （寻址方式）</strong></p><blockquote><p>寻址方式指的是诊断消息的传递方式，有两种寻址方式：</p><ul><li>物理寻址，即1对1通信，用于知道确切的被诊断ECU的地址；</li><li>功能寻址，即1对n通信，或者说<strong>广播</strong>发送，用于不知道确切的被诊断的ECU的地址，向一组或者全体ECU发送请求；![[Pasted image 20230919093318.png]]<br>地址概念：<br> -源地址：发送节点地址<br> -目标地址：接收节点地址<br> ![[Pasted image 20230919093709.png]]</li></ul></blockquote><p><strong>Response（响应）</strong></p><blockquote><p>tester请求诊断服务执行后，从ECU的返回结果。可以有两种结果：</p><ul><li>Positive Response，<strong>正响应</strong>，即诊断请求执行成功；</li><li>Negative Response，<strong>负响应</strong>，即诊断请求执行失败；</li></ul></blockquote><hr><p><strong>Service Identifier</strong> (SID) </p><blockquote><p>Service Identifier，诊断服务标识符，简称为SID，一字节的无符号整数，用来指代某个诊断服务。诊断协议为每个诊断服务都分配唯一SID，因此更方便协议的软件实现。同时，在开发过程中沟通更加方便。比如，ReadDataByIdentifier服务是去按照ID去读诊断数据，直接说22服务会更加便捷。</p></blockquote><ul><li>SID：Service Identifier，<strong>诊断服务ID</strong>。UDS本质上是一种定向的通信，是一种交互协议（Request&#x2F;Response），即诊断方给ECU发送指定的请求数据（Request），这条数据中需要包含SID。</li><li>如果是肯定的响应（<strong>Positive Response</strong>），回复<code>[SID+0x40]</code>，如请求10，响应50；请求22，响应62。</li><li>如果是否定的响应（<strong>Negative Response</strong>），回复<strong>7F+SID+NRC</strong>，回复的是一个声明。</li></ul><hr><p><strong>Data Identifier</strong></p><blockquote><p>数据标识符，简称DID，2个字节无符号整数的ID，用来标识ECU中存储的某个诊断数据单元。它的好处是当要读取某个单元的诊断数据时，只要读对应的DID就可以，不必知道数据的具体地址。即使当ECU中的数据地址发生变化时，改变DID和地址单元的映射关系即可，对于使用者来说DID屏蔽了具体实现细节，而将重点放在了数据本身。</p></blockquote><p><strong>Negative Response Code</strong></p><blockquote><p>可以简称为NRC，或者叫负响应码，是一个字节的无符号整数。它是诊断协议为每种执行失败的诊断服务分配的失败原因代号。</p></blockquote><p><strong>Sub-function</strong></p><blockquote><p>有些诊断服务可以支持不同的诊断子服务，sub-function就是用来定义这种子服务的，它将某一个服务细分为更为具体的服务，是一个字节的无符号整数。比如ECU Reset这个服务就有0x01，0x02，0x03等sub-function指代具体的reset方式。</p></blockquote><h1 id="二-UDS报文解读"><a href="#二-UDS报文解读" class="headerlink" title="二. UDS报文解读"></a>二. UDS报文解读</h1><h3 id="常用服务介绍"><a href="#常用服务介绍" class="headerlink" title="常用服务介绍"></a>常用服务介绍</h3><p>UDS的26种服务中，有7种很重要。它们分别是：</p><ul><li>$10 Diagnostic Session Control（诊断会话），</li><li>$14 Clear Diagnostic Information（清除诊断信息），</li><li>$19 Read DTC Information，</li><li>$22 Read Data By Identifier（通过ID读数据），</li><li>$27 Security Access（安全访问），</li><li>$2E Write Data By Identifier（通过ID写数据），</li><li>$3E Tester Present（待机握手）。</li></ul><p>![[Pasted image 20230922141323.png]]</p><hr><h4 id="10诊断会话"><a href="#10诊断会话" class="headerlink" title="$10诊断会话"></a>$10诊断会话</h4><p>$10包含3个子功能，</p><blockquote><ul><li>01 Default，</li><li>02 Programming，</li><li>03 Extended，</li></ul></blockquote><p><strong>ECU上电时，进入的是默认会话（Default）。如果您进入了一个非默认会话的状态，一个定时器会运转，如果一段时间内没有请求，那么到时间后，诊断退回到默认会话01</strong>。当然，我们有一个$3E的服务，可以使诊断保持在非默认的状态。</p><p><strong>报文包含4种类型</strong>，即</p><blockquote><ul><li>SID，</li><li>SID+SF（Sub-function），</li><li>SID+DID（Data Identifier）（读写用），</li><li>SID+SF+DID。</li></ul></blockquote><p>**NRC：Negative Response Code（否定响应码)**。如果ECU拒绝了一个请求，它会回应一个NRC。不同的NRC有不同的含义。</p><p>![[Pasted image 20230922141541.png]]</p><p>例子：以CAN总线网络举例。</p><p><strong>八个数据字节，第一字节被网络层占用</strong>。</p><ul><li>请求（Request）:</li></ul><blockquote><p>02 10 02 xx xx xx xx xx</p></blockquote><p>02中的0代表网络层单帧SF，2代表 数据域有2个字节；<strong>10是SID，02是子功能</strong>。</p><ul><li>肯定响应:</li></ul><blockquote><p>02 50 02 xx xx xx xx xx</p></blockquote><p>02同上，10+40表示对SID的肯定回复，02是子功能。</p><ul><li>否定响应:</li></ul><blockquote><p>03 7F 10 22 xx xx xx xx；</p></blockquote><p>03同上，7F表示否定响应，10是SID，22是NRC。</p><h4 id="3E待机握手"><a href="#3E待机握手" class="headerlink" title="$3E待机握手"></a>$3E待机握手</h4><p>$3E服务用于向服务器指示诊断仪仍然连接在网络上，之前已经激活的诊断服务功能可以仍然保持激活状态。</p><p>例子：</p><blockquote><p>02 3E 80 00 00 00 00 00，发送一个3E服务的报文，保持非默认会话状态。80表示无需回复。</p></blockquote><h4 id="27安全访问"><a href="#27安全访问" class="headerlink" title="$27安全访问"></a>$27安全访问</h4><p>27服务，加上一个子服务，再加上一个钥匙，这样的服务请求可以进行解锁。</p><p>比如下面的例子，2n-1是某个子服务，通过首轮种子的请求，首轮ECU会返回67+01+AA+BB+CC+DD，AA~DD就是种子了。之后第二轮，诊断端会利用种子进行运算（利用整车厂的算法），生成k1（不一定是1个字节），那么发送请求，27+02+[k1]。ECU同样也会通过种子算出k2。当k1和k2匹配时，解锁（Unlocked）成功。</p><ul><li>例子：</li></ul><blockquote><p>Rx： 02 27 05 00 00 00 00 00 安全访问，05子功能<br>Tx： 07 67 05 08 27 11 F0 77 肯定响应，<strong>回复了对应安全级别的种子</strong><br>Rx： 06 27 06 FF FF FF FF 00 <strong>发送密钥</strong>，4个FF。注意06是与05成对使用的。<br>Tx： 03 7F 27 78 00 00 00 00 否定响应，7F+27+NRC<br>Tx： 02 67 06 00 00 00 00 00 肯定响应，通过安全校验</p></blockquote><h4 id="22读数据"><a href="#22读数据" class="headerlink" title="$22读数据"></a>$22读数据</h4><p>$22读数据，<br>Request（请求）：</p><blockquote><p>22+DID（Data Identifier，通常是两个字节）</p></blockquote><p>Response（响应）：</p><blockquote><p>62+DID+Data</p></blockquote><p>DID有一部分已经被ISO 14229-1规定了。比如0xF186就是当前诊断会话数据标识符，0xF187就是车厂备件号数据标识符，0xF188就是车厂ECU软件号码数据ID，0xF189就是车厂ECU软件版本号数据标识符。</p><h4 id="2E写数据"><a href="#2E写数据" class="headerlink" title="$2E写数据"></a>$2E写数据</h4><p>$22写数据，<br>Request（请求）：</p><blockquote><p>2E+DID+Data</p></blockquote><p>Response（响应）：</p><blockquote><p>6E+DID</p></blockquote><p>注意，比如0xF186这个DID不支持直接写入数据，需要用$10来进行会话转换。也就是说，<strong>对于写数据的请求，一般来说需要在一个非默认会话，或解锁的状态下才能进行</strong>。</p><h4 id="19-读DTC"><a href="#19-读DTC" class="headerlink" title="$19 读DTC"></a>$19 读DTC</h4><p>DTC（diagnostic trouble code）：如果系统检测到了一个错误，它将其存储为DTC。DTC可表现为：一个显而易见的故障:通讯信号的丢失（不会使故障灯亮起）；排放相关的故障；安全相关的错误等。DTC可以揭示错误的位置和错误类型。通常DTC占用3个字节，OBD II占用两个字节。</p><p><strong>故障码包括四个大类，分别是PCBU，P是powertrain动力系统，C是Chassis底盘，B是Body车身，U是network通信系统。一个DTC信息占用4个字节。最后一个字节是DTC的状态</strong>。前两个字节是我们熟知的类似P0047的故障码。</p><table><thead><tr><th>DTCHighByte</th><th>DTCMiddleByte</th><th>DTCLowByte</th><th>DTCStatus</th></tr></thead><tbody><tr><td>Byte 1</td><td>Byte 2</td><td>Byte 3</td><td>Byte 4</td></tr></tbody></table><p>$19 拥有28个子服务（Sub-Function）。常用的子服务有02（通过DTC状态掩码读取DTC），04（读取快照信息），06（读取扩展信息），0A（读ECU支持的所有DTC数据）。</p><h4 id="14清除DTC"><a href="#14清除DTC" class="headerlink" title="$14清除DTC"></a>$14清除DTC</h4><p>清除（复位）DTC格式，它可以改变DTC的状态。3个FF代表清除所有DTC。</p><blockquote><p>Request：14+FF+FF+FF；<br>Response：54 。</p></blockquote><hr><h3 id="诊断报文解析"><a href="#诊断报文解析" class="headerlink" title="诊断报文解析"></a>诊断报文解析</h3><p>UDS 的诊断数据的发送与接收都是基于CAN，所以每个数据流都包含基本的CAN Message 的架构</p><blockquote><p>CAN Message &#x3D;CAN ID + CAN DATA</p></blockquote><p>根据上篇UDS文章的叙述，每一个PDU 包含控制信息PCI,数据信息Data.</p><p>![[Pasted image 20230922144315.png]]</p><p>网络层 PDU（协议数据单元）PCI（协议控制信息）格式：具体如下图所示：</p><p>![[Pasted image 20230922144350.png]]</p><p>综上所述，<code>N_PDU =N_PCI+N_DATA</code>, <code>N_PCI</code>的值主要集中的<strong>前三个字节</strong>，<code>N_DATA</code>值主要集中在<strong>后面7位字节</strong>。其中，</p><ul><li><code>SF_DL</code> 代表单帧中数据字节数（取值0-7），</li><li><code>FF_DL</code>代表 连续帧中的数据字节数(12bit可表四8~4095)，</li><li><code>SN</code>代表此帧为连续帧中的第几帧，（0、1、2…E、F、0、1…）</li><li><code>FS</code>流控制帧，有三种状态：继续发送0、保持等待1、数据溢出2</li><li><code>BS</code>规定发送端允许持续传输连续帧数目的最大值（0~255），</li><li><code>STmin</code>限定连续帧相互之间所允许的最小时间间隔。</li></ul><p>先面用连个例子进行说明，请参考！</p><h4 id="例子-1—-单帧的数据传输与接收"><a href="#例子-1—-单帧的数据传输与接收" class="headerlink" title="例子 1— 单帧的数据传输与接收"></a>例子 1— 单帧的数据传输与接收</h4><blockquote><p>数据发送： 02 27 09<br>数据反馈： 03 7F 27 7E —&#x3D;&#x3D;否定的响应&#x3D;&#x3D;（Negative Response），回复&#x3D;&#x3D;7F+SID+NRC&#x3D;&#x3D;，回复的是一个声明</p></blockquote><blockquote><p>数据发送： 02 10 40<br>数据反馈： 06 50 40 00 32 01 F4 —&#x3D;&#x3D;肯定的响应&#x3D;&#x3D;（Positive Response），回复[&#x3D;&#x3D;SID+0x40&#x3D;&#x3D;]，就是请求10，响应40；回复的是一组数据</p></blockquote><p>由于这个数据发送与接收都是单帧传输，所以第一个数据的高四位均为0，四个数据流中的第一个字节的低四位，02，03，02，06代表的为此帧数据含有几个字节，多余的数据位都用 00或者AA行填充。</p><h4 id="例子2-—-多帧的数据接收与传输"><a href="#例子2-—-多帧的数据接收与传输" class="headerlink" title="例子2 — 多帧的数据接收与传输"></a>例子2 — 多帧的数据接收与传输</h4><p>[图片上传失败…(image-b5e84b-1538824826939)]</p><blockquote><p>数据发送：</p><ul><li>06 19 04 00 01 00 00 00</li></ul></blockquote><blockquote><p>数据反馈：</p><ul><li>10 1E 59 04 00 01 00 27</li><li>30 00 00 00 00 00 00 00</li><li>21 00 0B FF FF FF FF FF</li><li>22 FF FF FF FF FF FF FF</li><li>23 FF FF FF FF FF FF FF</li><li>24 FF FF FF AA AA AA AA</li></ul></blockquote><p>数据发送为单帧，所以06代表发送的数据中含有6个字节，</p><p>回复为Positive Response，为连续帧。</p><ul><li>10中的1代表连续帧的首帧，&#x3D;&#x3D;01E代表此连续帧含有30个字节&#x3D;&#x3D;，</li><li>30代表此连续帧的流控制帧，</li><li>21，22，23，24代表连续帧中的第几帧，21代表第一帧，22代表第二帧，依此类推，其中AA为填充位。</li></ul><h2 id="DTC介绍"><a href="#DTC介绍" class="headerlink" title="DTC介绍"></a>DTC介绍</h2><p>![[Pasted image 20240305151213.png]]</p><h3 id="bit-0，testFailed，最近的一次测试是否失败"><a href="#bit-0，testFailed，最近的一次测试是否失败" class="headerlink" title="bit 0，testFailed，最近的一次测试是否失败"></a>bit 0，testFailed，最近的一次测试是否失败</h3><p>0：没有失败，1：失败了</p><p>就是反映最近一次故障检测的结果。</p><p>故障检测是指控制器会在特定时间运行一段程序来检查一下系统的状态（例如：控制器的一个IO口，正常电压范围是5~10V，然后控制器的程序里，每隔一分钟会去读一下这个IO口的电压，如果在正常电压范围内，就算测试通过，反之，测试失败）。有些测试在一个cycle中运行一次，有些测试可能每几十毫秒就运行一次，考虑简单的情况，假设测试是每一分钟运行一次。</p><h3 id="bit-1，testFailedThisOperationCycle，当前循环，故障检测是否失败"><a href="#bit-1，testFailedThisOperationCycle，当前循环，故障检测是否失败" class="headerlink" title="bit 1，testFailedThisOperationCycle，当前循环，故障检测是否失败"></a>bit 1，testFailedThisOperationCycle，当前循环，故障检测是否失败</h3><p>0：没有失败，1：失败了至少一次</p><p>就是记录当前cycle中是否发生过故障检测失败的情况，每次进入新的cycle都会先置0。</p><p>Operation cycle会有不同的设计（例如：1. ECU上下电之间的时间，2. 发动机点火和熄火之间的时间，3. 发动机运行的时间，4. 发动机没有运行的时间），对于一般ECU来说，考虑简单的情况，就取ECU上电和下电之间的时间（如果ECU是个灯，就是开灯和关灯之间的时间）作为一个operation cycle。</p><h3 id="bit-2，pending，是否有待处理的故障"><a href="#bit-2，pending，是否有待处理的故障" class="headerlink" title="bit 2，pending，是否有待处理的故障"></a>bit 2，pending，是否有待处理的故障</h3><p>0：没有，1：有</p><p>该位的另一种表述是在当前cycle或者上一个cycle中是否检测到故障（因为如果上一个cycle没有检测到且当前cycle也还没有检测到，那么bit 2应该是0），和bit 1的置1条件相同，测试失败就会置1，但是bit 2在进入新的cycle之后不会清零，只有在cycle完成且测试完成且没有检测到故障时才会置零。</p><h3 id="bit-3，confirmed，故障是否确认"><a href="#bit-3，confirmed，故障是否确认" class="headerlink" title="bit 3，confirmed，故障是否确认"></a>bit 3，confirmed，故障是否确认</h3><p>0：没有确认，1：确认了</p><p>故障的确认是指故障发生的次数超过设定的阈值，对于non-OBD（OBD和non-OBD是指控制器负责的部件是否与车辆排放相关，常见的OBD控制器有发动机控制器，变速箱控制器等）的控制器，这个值是1，那么在这种情况下，当DTC pending的时候，也就相当于confirmed了。</p><h3 id="bit-4，testNotCompletedSinceLastClear，清除DTC之后，是否完成过故障检测"><a href="#bit-4，testNotCompletedSinceLastClear，清除DTC之后，是否完成过故障检测" class="headerlink" title="bit 4，testNotCompletedSinceLastClear，清除DTC之后，是否完成过故障检测"></a>bit 4，testNotCompletedSinceLastClear，清除DTC之后，是否完成过故障检测</h3><p>0：完成了，1：没有完成</p><p>bit 4 到 bit 7都比较容易理解。</p><h3 id="bit-5，testFailedSinceLastClear，清除DTC之后，故障检测是否失败"><a href="#bit-5，testFailedSinceLastClear，清除DTC之后，故障检测是否失败" class="headerlink" title="bit 5，testFailedSinceLastClear，清除DTC之后，故障检测是否失败"></a>bit 5，testFailedSinceLastClear，清除DTC之后，故障检测是否失败</h3><p>0：没有失败，1：失败过</p><p>注意0的情况下，也可能是故障检测没有完成。</p><h3 id="bit-6，testNotCompletedThisOperationCycle，当前循环，是否完成过故障检测"><a href="#bit-6，testNotCompletedThisOperationCycle，当前循环，是否完成过故障检测" class="headerlink" title="bit 6，testNotCompletedThisOperationCycle，当前循环，是否完成过故障检测"></a>bit 6，testNotCompletedThisOperationCycle，当前循环，是否完成过故障检测</h3><p>0：完成了，1：没有完成</p><h3 id="bit-7，warningIndicatorRequested，ECU是否请求点亮警告灯"><a href="#bit-7，warningIndicatorRequested，ECU是否请求点亮警告灯" class="headerlink" title="bit 7，warningIndicatorRequested，ECU是否请求点亮警告灯"></a>bit 7，warningIndicatorRequested，ECU是否请求点亮警告灯</h3><p>0：没有请求，1：请求了</p><p>该位是否置1在于，整车厂在设计时，已经确定了每个DTC在被触发时是否会亮起警告灯，亮哪一个警告灯，以及故障提示的内容。如果某个DTC没有设计为会触发亮灯，那么这个DTC被触发时，bit 7也应该始终为0。</p><h2 id="示例"><a href="#示例" class="headerlink" title="示例"></a><strong>示例</strong></h2><p>对照上图，试着推断几个实际的故障码的状态位。</p><blockquote><p>00100111(最左边是bit 7)</p></blockquote><p>bit 7，没有对应的故障警告灯显示</p><p>bit 6，当前循环完成了故障检测</p><p>bit 5，清除DTC之后故障检测失败过</p><p>bit 4，清除DTC之后故障检测完成过</p><p>bit 3，故障没有确认（这里说明这个DTC要求的确认阈值是大于1的）</p><p>bit 2，当前cycle或上一个cycle检测到了故障</p><p>bit 1，当前cycle检测到故障</p><p>bit 0，上一次检测中检测到了故障</p><blockquote><p>00101111(最左边是bit 7)</p></blockquote><p>与上一个DTC的状态位，只有bit 3有区别。</p><p>bit 3，故障确认了。</p>]]></content>
      
      
      <categories>
          
          <category> 编程 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 原创 </tag>
            
            <tag> 编程 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
